{
  "name": "sonestatour-api",
  "version": "1.0.0",
  "description": "API server for SonestaTour travel website",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "seed": "psql $DATABASE_URL -f seed.sql",
    "migrate": "psql $DATABASE_URL -f schema.sql"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pg": "^8.11.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
